services:
  - type: web
    name: yardbids-preview
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: STRIPE_MODE
        value: test
